@model CarLoc.Models.Status

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Status</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Descricao)
        </dd>
    </dl>
</div>

<hr />

<h4>Veículos com este Status</h4>

@if (Model.Veiculos != null && Model.Veiculos.Any())
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Placa</th>
                <th>Modelo</th>
                <th>Ano</th>
                <th>Categoria</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var veiculo in Model.Veiculos)
            {
                <tr>
                    <td>@veiculo.Placa</td>
                    <td>@veiculo.Modelo</td>
                    <td>@veiculo.Ano</td>
                    <td>@veiculo.Categoria?.Nome</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p><em>Não há veículos com este status atualmente.</em></p>
}

<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
